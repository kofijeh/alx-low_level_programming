#include "main.h"

/**
 * _atoi - I don't know what this is for. Creating an empty file with it
 * @s: character in the prototype
 * Return: unspecified
 */
int _atoi(char *s)
{
	*s = 'H';

	return (0);
}
